/*package Circuit;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.*;

public class VariableTest {

    private Variable variable;

    @BeforeEach
    public void setUp() {
        variable = new Variable(QuadBool.NOTHING);  // Crée une nouvelle variable initialisée à NOTHING
    }

    @Test
    public void testVariableInitialValue() {
        // Vérifie que la valeur initiale de la variable est "NOTHING"
        assertEquals(QuadBool.NOTHING, variable.getValue());
    }

    @Test
    public void testSetValue() {
        // Définit une nouvelle valeur pour la variable
        variable.setValue(QuadBool.TRUE);
        // Vérifie que la valeur de la variable a bien été modifiée
        assertEquals(QuadBool.TRUE, variable.getValue());

        // Modifie la valeur à FAUX
        variable.setValue(QuadBool.FALSE);
        // Vérifie que la valeur est maintenant FAUX
        assertEquals(QuadBool.FALSE, variable.getValue());
    }

    
}
*/
